{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/${config:env.directory}${config:env.project}/bin/Debug/${config:env.framework}/${config:env.project}.dll",
            "args": [],
            "stopAtEntry": false,
            "console": "integratedTerminal",
        },
    ]
}
